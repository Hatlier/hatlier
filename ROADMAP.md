# Hatlier ロードマップ & 見える化

開発の「いま・次・あと」をここに置く。  
**リリースノートより、予定と優先度の方が先** — この文書を正本にする。

画面左上の `vX.Y.Z` はこの ROADMAP へリンクする。

---

## いまの全体像（見直し 2026-07-26）

MVPの「直す → 保存 → 書き出す」はだいたい揃った。  
**プロダクト本線（実装の軸）はこれまでどおり A / B / C**。ここに「何を作るか」の契約がある。  
並行して、**見せ方・勝ち筋・AIとの役割分担**の検討を本ファイルに積む（下の「勝ち筋の検討」）。実装を置き換えるものではなく、優先度と伝え方の羅針盤にする。

| 軸 | ひとこと |
|---|---|
| **A. 手触り（MVP-D）** | 迷わない・壊れない・言ってることが分かる |
| **B. フォーマット拡張** | 縦スクロール一択を超え、レスポンシブを保ったまま表現の自由度を上げる |
| **C. 出力プロファイル** | 作り始めに「どんなHTMLにしたいか」を選び、部品・テンプレをそれに合わせて制限する |
| **見せ方（並行）** | 「何ができるか」が一瞬で伝わる実演・最初の5分体験（機能増の代わりに伝える） |

**マイテンプレート**は B/C と同じレイヤ（再利用・フォーマットの延長）。置き場導線（Drive等）より、フォーマット議論とセットで扱う。

---

## 運用

| 置き場 | 役割 |
|---|---|
| **`ROADMAP.md`（本ファイル）** | 予定と優先度の正本 |
| **`SPEC.md`** | 思想・原則・詳細仕様 |
| **GitHub Releases** | 大きな区切りだけ（任意） |
| **Commit** | 何を変えたか |

毎 push にリリースノートは付けない。作業前に本ファイルの行を動かし、触れる区切りでだけ `HATLIER_VERSION` を上げる。

### テスト（UI破綻チェック）

製品本体は依存ゼロの `hatlier.html`。テストは **ローカルで育てる**（毎 push の CI は今はやらない）。

| いつ | 何を |
|---|---|
| UI・パネル・ブロック周りを触ったあと | `hatlier.html?selftest`（依存ゼロ・最速） |
| もう少し厚く見るとき | `npm test`（Playwright）または `python tests/selftest.py` |
| 開発者が増えたら | GitHub Actions へ昇格（PR / `main` のみ、またはパスフィルタ） |

入口: `window.__hatlier` / `window.__hatlierSelfTest()`（本体の挙動は変えないフック）。

---

## バージョン

| 版 | 日付 | ひとこと |
|---|---|---|
| **0.6.0** | 2026-07-27 | Format Core 実装着手: `format`/`schema` 明示、未知部品保持、parse/migrate/render/roundTrip API。仕様正本 [`FORMAT-CORE-v1.md`](./FORMAT-CORE-v1.md) |
| **0.5.9** | 2026-07-27 | 構図 `split`（メイン＋サイド）。幅比・左右は列挙のみ。モバイルは縦折り。自由配置なし |
| **0.5.8** | 2026-07-27 | 画像・動画の表示枠（全幅/広め/半分/小さめ＋寄せ）。角ドラッグはプリセットにスナップ。自由配置は見送り |
| **0.5.7** | 2026-07-27 | カレンダー月↔タイトル連動、タブ4 variants、タブ/トグル/FAQに画像スロット（Notion級入れ子は未） |
| **0.5.6** | 2026-07-26 | QR/バーコード即時生成、カレンダーはマス直編集、ガント％可視編集、ページ内検索はエディタでも動作＋向きヒント、対訳ブロックはパレット非表示 |
| **0.5.5** | 2026-07-26 | 未着手部品一括: tabs / calendar / gantt / clock / audio / QR・barcode / pagesearch / langs。色覚シミュレーション、画像クロップ、長尺メディア→ZIP誘導。selftest 拡充 |
| **0.5.4** | 2026-07-26 | カメラ／画面録画の再生不具合修正（data-URI の codecs カンマ問題）。全部品共通オプション（`space`／`variant`）骨格 |
| **0.5.3** | 2026-07-26 | ページ幅（default/wide/full）。ヒーロー見出しバリアント＋背景プリセット。新規ウィザード（目的＋出口）。部品props互換ルールを FORMAT-v2 に明文化 |
| **0.5.2** | 2026-07-26 | カルーセル「表示」ボタンが開かない不具合を修正。UI破綻の自動チェック（`?selftest` / `window.__hatlierSelfTest()` / Playwright・Python ランナー）を追加 |
| 0.5.1 | 2026-07-24 | カルーセル強化: 両方向peek、番号/ループ（表示）、スライドに画像・リンク |
| 0.5.0 | 2026-07-22 | マイテンプレ（localStorage・容量上限）＋カルーセル（CSS scroll-snap） |
| 0.4.0 | 2026-07-22 | 出力プロファイル骨格（safe/standard/rich）。デザインで説明付き選択・パレットフィルタ・書き出しに `data-profile` |
| 0.3.3 | 2026-07-22 | 手触り D1–D4。[`FORMAT-v2.md`](./FORMAT-v2.md) 仕様メモ |
| 0.3.2 | 2026-07-22 | 書き出し前にファイル名編集（`YYYYMMDDhhmm.タイトル`） |
| 0.3.1 | 2026-07-22 | PNG/動画ラスタライズ修正。MP4優先 |
| 0.3.0 | 2026-07-22 | 書き出し拡充、バージョン導線 |
| 0.2.x〜 | — | 貼る / 保存 / HTML・ZIP / 公開 |

---

## Now — 手触り（MVP-D）

- [x] D1 保存＝ブラウザ内 / 書き出し＝ファイル（文言）
- [x] D2 未書き出しの新規確認
- [x] D3 貼る結果メッセージ固定
- [x] D4 フリー編集中チップ＋案内
- [ ] D5〜 初回案内の強化、モバイル整理など（低い〜中）
- [x] **D-visual（一部）** — 幅選択・ヒーロー版・背景プリセット・新規ウィザード（0.5.3）。テーマ全体の「サイトっぽさ」磨きは継続
  - [x] 1. コンテンツ幅 `doc.width`
  - [x] 2. ヒーロー `props.variant`
  - [x] 3. ヒーロー背景プリセット（幾何 data-URI ＋ 写真URL）
  - [x] 5. 新規ウィザード（目的＋出口 → テンプレ）
  - [ ] 既定テーマ／ブロック質感のさらなる磨き（継続）

仕様の大きな次: [`FORMAT-CORE-v1.md`](./FORMAT-CORE-v1.md)（フォーマット共通契約）／拡張は [`FORMAT-v2.md`](./FORMAT-v2.md)

---

## 守るもの（出口・再編集・前方互換）— 機能増の前に

Hatlier が捨てない契約。新機能はこの枠の中で選ぶ。

| 原則 | 意味 |
|---|---|
| **出口が先** | CSPが強いホストでも壊れない選択ができる（`safe`＝JSなし）。「作ってから公開先で死ぬ」を防ぐ |
| **書き出したHTMLは再編集できる** | Hatlierで開いて手動編集が第一級。表示専用ブラックボックスにしない |
| **依存ゼロの1ファイルを基本** | ただしメディア肥大はフェールセーフで ZIP／外出しへ逃がす（下のメモ5） |
| **前方互換** | 旧文書は必ず開ける。破壊的変更は `data-hatlier-version` / `doc.version` を上げ、migrate で吸収。未知ブロックは落さず「未対応」表示＋データ保持 |
| **プロファイルで部品を制限** | 欲しい機能全部を全プロファイルに載せない。`needs` タグで `safe` / `standard` / `rich` に振り分ける |

「カレンダーも時計もカメラも」は魅力だが、**全部を safe に押し込むと出口契約が壊れる**。すみ分けは Track C が正本。

---

## 検討メモ（2026-07-26）— 気になっていること

優先の仮置き: **見た目（1〜3）→ 新規ウィザード強化（5の入口）→ メディア健全化（4・5）→ 部品（6）→ Skillメンテ（7）**。  
実装は仕様合意後。ここはバックログの地図。

### 1. コンテンツ幅（余白）をユーザが選ぶ — **0.5.3 実装**

`doc.width`: `default` | `wide` | `full`。デザインパネル＋新規ウィザード。旧文書は `default`。

### 2. タイトル（ヒーロー）のバリエーション — **0.5.3 実装**

`hero.props.variant`: `display` | `quiet` | `banner` | `minimal`。ツールバー「見出:」で切替。

### 3. ヒーロー背景の素材ライブラリ — **0.5.3 実装**

幾何（data-URI・オフライン可）＋写真URLプリセット。「背景画像」から選択。`props.bg` に書くだけ（新キーなし）。

### 4. 画像・動画: 端末カメラ／画面録画＋クロップ

あると便利。ただし能力タグが必要。

| 機能 | 編集時 | 書き出し後の文書 | profile |
|---|---|---|---|
| カメラ撮影・画面録画 | エディタの機能（ブラウザAPI） | 成果は静的メディア | 編集UX。出力HTMLに録画APIは載せない |
| 画像クロップ | エディタ | クロップ済みを埋め込み／参照 | 全profile可 |
| 動画の時間トリム | エディタ | トリム済みを埋め込み／参照 | 容量に注意 → メモ5 |

編集用ツールと、公開HTMLに残るJSは分離する（出口契約）。

### 5. 動画と1HTML／ZIPの割り切り ＋ 新規時の出口確定

- base64動画の現状は破綻しやすい。**フェールセーフ必須**（動画ポリシーは未実装・継続）
- 「新規でテンプレを選ぶとき」に **目的＋出口** を先に確定 — **0.5.3 でウィザード実装**
  - 推奨文付き。テンプレ適用時に `profile` / `width` を反映
  - デザインパネルでもあとから変更可

### 6. 部品バックログ（プロファイル付きで）— 状況 2026-07-26

| 候補 | 状態 | 仮 needs | メモ |
|---|---|---|---|
| トグル（開閉） | **既存**（`details`） | `[]` | safe 可 |
| FAQ | **既存** | `[]` | safe 可 |
| タブ | **0.5.5**（CSS radio） | `[]` | Track B |
| カレンダー／簡易ガント | **0.5.6** | `[]` | カレンダーはマス直編集（一覧廃止）。ガントは開始%/終了%を可視編集 |
| アナログ／デジタル時計 | **0.5.5** | 静的=`[]`／`live`=`js` | ツールバーで切替 |
| 音声（端末録音）＋サムネ | **0.5.5** | 編集時のみ | カメラ録画と同系統 |
| QR／バーコード | **0.5.6** | `[]`（画像化） | テキスト入力で即時生成（生成ボタン廃止） |
| ページ内検索 | **0.5.5+** | `js` | **standard向き**。エディタ内でもハイライト動作。safeではパレット非表示 |
| 色覚多様性パレット | **0.5.5** | デザイン設定 | `doc.cvd`／近似フィルタ |
| i18n／言語切替 | **Later**（0.5.5で対訳ブロック試作→パレット非表示） | — | 部品単位は扱いにくい。文書全体のi18nは別設計 |
| カメラ／画面録画 | **既存**（0.5.x） | 編集時のみ | **data-URI MIME 正規化を 0.5.3+ で修正** |
| 画像・動画クロップ | **0.5.5** | 編集時 | 画像は切り抜き／動画はポスターフレーム |
| 長尺動画→ZIP | **0.5.5** | 書き出しポリシー | 1.5MB超でZIP誘導 |

共通オプション（`space` / `variant`）骨格は **0.5.3+** で導入。ヒーロー・ステップは `variants` 済み。他部品は余白ボタン共通。

### 7. 生成AI用プロンプトのメンテ（＝実質 Skill）

- 公式プロンプト／Skill を **バージョン付き** で保つ（フォーマット・profile・部品に追従）
- 「Hatlier用にHTMLを出す」契約を一文で固定し、examples とデモ台本と同期
- コミュニティ共有は Later。ソロ期はリポ内 Skill が正本

---

## 前方互換の設計メモ（継続）

1. **読むときは寛容、書くときは明示** — 未知の `type` / `props` / `shell` は捨てず保持。UIは「この版では未対応」
2. **migrate は純関数** — `load` 時に旧→現行形。保存し直すまでファイルは旧のままでもよい
3. **profile を厳しくするとき** — 非対応部品を一覧し、削除か profile 維持か確認（既存合意）
4. **メディア方針変更**（1HTML禁止閾値など）— 旧文書は開ける。書き出し時に警告＋ZIP誘導
5. 破壊的変更のたびに `HATLIER_VERSION` と ROADMAP バージョン表を更新

---

## Track B — フォーマット拡張（デザイン自由度）

### 問題意識

いまの契約は **ブロックの縦積み** が強い。レスポンシブは保ちやすいが、

- 横スクロール帯
- カルーセル
- サイドバー
- ハンバーガー／モバイルナビ

といった「ページとしての構図」が出しにくい。

### 方針（**合意・非交渉** — 2026-07-27）

Hatlier の強みは **崩れない・直せる・1HTML**。SPO 等の制約付き環境と同じ発想で、表現は枠で増やす。

1. **レスポンシブは捨てない**（モバイルで情報が欠けない）
2. **自由配置キャンバスは禁止**（Figma化しない。絶対座標・重ね順ドラッグ・自由px配置は永続データにしない）
3. 表現の自由度は **レイアウト枠（shell / region）と部品の列挙オプション** だけで増やす  
   例: `stack`（現行）, **`split`（0.5.9）**, 次候補 `sidebar`, 既存の `carousel` / `tabs` / 画像 `frame`
4. 既存の縦積みドキュメントは **互換のまま開ける**（バージョン付きフォーマット）
5. 新しい枠を足すときは必ず: **種類は少数**／**モバイル折りルールを枠に固定**／**枠内は縦積み**／**自由座標を持たない**

#### 横並び・サイド置きの判定表

| やり方 | 破綻しやすさ | 判定 |
|---|---|---|
| 自由座標（px / ドラッグ置き） | 高い | **禁止（非交渉）** |
| 画像・動画の幅プリセット | 低い | **済（0.5.8）** |
| `split` / `sidebar`（決まった枠） | 中（折りルール固定なら可） | **`split` 済（0.5.9）**。`sidebar` は後 |
| 既存 `cols` を部品スロット化 | 中 | shell とセットで検討 |

### 検討バックログ（実装は仕様合意後）

- [x] **構図 `split`（0.5.9）** — メイン縦積み＋サイド1列。幅比 `2-1`/`1-1`/`1-2`、左右 `end`/`start`。モバイルは縦折り。自由配置なし
- [x] **Format Core v1.0（文書）** — [`FORMAT-CORE-v1.md`](./FORMAT-CORE-v1.md)。不変条件・正本JSON・互換API
- [x] **Format Core 実装（0.6.0）** — schema 明示・未知部品プレースホルダ保持・Core API。入れ子書出は後続
- [ ] フォーマット v2 の骨子（入れ子 `doc.components` 書出、JSON Schema 同梱など）
- [x] 部品: カルーセル（CSS scroll-snap, 0.5.0）
- [x] 部品: タブ（CSS radio, 0.5.5）
- [ ] 部品: サイドバー付きシェル、ハンバーガー（`split` の後）
- [ ] 横スクロール・セクション（レスポンシブ時は縦に折るルール）
- [ ] AIプロンプトを v2 構造に追従
- [x] マイテンプレ最小（0.5.0）

---

## Track C — 出力プロファイル（作り始めの制約）

### 問題意識

書き出し形式や公開先で **許されるものが違う**。  
後から「CSP強いホストに上げたらスクリプトが動かない」より、**プロファイルで部品パレットとテンプレをフィルタする**方が UX として正しい。

詳細の合意は [`FORMAT-v2.md`](./FORMAT-v2.md) §3・§8。

### プロファイル（合意）

| ID | 呼び方（仮） | 制限 |
|---|---|---|
| `safe` | **いちばん無難**（**デフォルト**） | HTML+CSSのみ。**JSなし**。超強いCSPでも動く思想 |
| `standard` | ややゆるめ／ふつうの静的ホスト | 軽量JS可（現行相当）。fetchなし |
| `rich` | 拡張（Later） | fetch等。初期UIに出さなくてもよい |

CSPは実質 **強（safe）／弱（standard）**。選び方はユーザが迷わない文言に。

### UX（合意）

1. デフォルトは `safe`。**新規時の選択はスキップ可**（サジェストは急がない）
2. `doc.profile` / `data-profile` に保存
3. 部品パレット（場所クリック → 部品選択）で **使えるものだけ出す**
4. 厳しく変更するときは非互換部品を出して確認
5. **強化候補（2026-07-26）:** 新規を「目的＋出口」の短いウィザードに育てる（推奨文付き）。詳細は上の検討メモ5

### 部品へのタグ付け

各 `REG` に `needs: [] | ["js"] | ["media"] | ["fetch"]` など。  
`safe` は空のみ許可。

---

## Track D — マイテンプレート（B/C と同列）

| 内容 | メモ |
|---|---|
| いまの文書を「マイテンプレ」として **localStorage** に残す | **済（0.5.0）** |
| 上限は **メモリ量ベース**（件数固定より）。ユーザが変えられる | **済** — デザイン「ブラウザ設定」で KB 変更 |
| 新規時に公式テンプレと並べて選べる | **済** |
| プロファイル付きで保存（`safe` の雛形、など） | 文書スナップショットに含まれる |
| エクスポート／インポート（テンプレの受け渡し） | 未（Step 2寄りでも可） |

---

## Next（手触りの直後〜並行）— 実装本線

[`FORMAT-CORE-v1.md`](./FORMAT-CORE-v1.md) をフォーマット正本に固定。[`FORMAT-v2.md`](./FORMAT-v2.md) の §8 は合意済み。**Phase 0–1（0.4.0）・マイテンプレ＋carousel（0.5.0）・split（0.5.9）実装済み**。次:

1. ~~**次の一本を堅実に:** `split`~~ **済（0.5.9）**
2. ~~tabs（CSS / details 優先）~~ **済（0.5.5）**
3. ~~Format Core に沿った `schema` 明示と unknown preservation の実装強化~~ **済（0.6.0）** — 入れ子書出・JSON Schema は後続
4. `needs:["js"]` 部品が増えたときの見え方磨き
5. マイテンプレの入出力（JSON受け渡し）
6. `sidebar` / `topnav` は `split` の経験を見てから

書き出しの磨き（PDF余白、AGIF、置き場導線）は隙間で可。手触り D5〜 も同様。

**ここがエンジニアリングの本線。** 下の「勝ち筋」はこれを捨てない前提で、伝え方と体験の優先度を足す。

---

## 勝ち筋の検討（2026-07-26）— 伝え方・世界観

> 実装トラック（A/B/C）を置き換えない。**「何を増やすか」より「何を10倍速く・何が一瞬で伝わるか」**のメモ。  
> SPEC の本線（手動編集が第一級／HTMLを資産にする／1ファイル完結）と矛盾しない範囲で進める。

### 立ち位置（仮）

| 言い方 | 意味 |
|---|---|
| 売っているもの | HTMLエディタではなく、**AIと人が一緒に Web を仕上げる場所** |
| ポジション | AIが ~90点まで作る → **Hatlierで最後の手触りを100点にする** |
| 軸の一文（候補） | `Generate with AI. Finish with Hatlier.` / `AI creates. You craft.` |
| 競合の見方 | Canva / Figma / Bento 単体ではなく、**「HTMLを作るすべての手段」** |

キャッチやデモは上記に揃える。機能リストを先に増やして「何でもできるツール」に見せない。

### Bento.page から学ぶこと（競合ではなく先輩）

共通: HTML1枚・インストール不要・ローカル／オフライン寄り・AI編集・ファイル＝成果物。  
違い: Bento は **プレゼン（ドキュメント）**、Hatlier は **Webコンテンツそのもの**（LP・教材・ゲーム・SVG・Canvas など）。守備範囲は広いが、だからこそ最初のメッセージを絞る。

| 取り入れたい（検討） | いまの Hatlier との関係 |
|---|---|
| 編集画面の異常なシンプルさ（詳細は後） | 手触り A・パレット／プロファイル C と親和 |
| HTML内に編集用データ（JSON/DSL）を埋める | FORMAT-v2・マイテンプレ・AI連携の設計候補。**表示用HTMLと編集用データの分離**は要検討（既存の「HTMLそのものが資産」と両立する形で） |
| AIは構造化データだけ触る | 「貼る」導線の延長。手動編集は第一級のまま |
| 部品中心 UI | Track B の部品増と両立。最初から全 Web API を出さない |

やらない寄り（既存どおり）: 自由配置キャンバスの Figma 化、プレゼン専用ツール化。

### いま弱いところ（認識）

- 「何ができるの？」が一瞬で伝わらない（エディタ／ノーコード／AI／SVGが少しずつ見える）
- 機能がまだ少ない **以上に**、動線と実演が足りない
- README だけでは「これはいいかも」が起きにくい

### 優先度の仮説（見せ方側）

実装本線（Next）と **並行**。機能増の代わりに「これすごい」を増やす。

**Phase S1 — これすごい（最優先候補）**

1. **デモ動画（ストーリー）** — READMEより効く。機能紹介ではなく世界観。
2. **最初の5分体験** — テンプレ選択 →（AI生成）→ 手で直す → 書き出し／公開まで。
3. **examples/** — クリックで即編集できる完成例（lp / blog / portfolio / game / education…）。
4. **AI Skill（プロンプト／ワンクリック改善）** — 「夏っぽく」「余白を整える」等。コミュニティ共有は後。

**デモ1本目（確定寄り）**

```
LLM → HTML生成 → Hatlierで開く → 「ここ直したい」→ ダブルクリックで手動編集 → 書き出し
```

見せる一言: **AIが作る。あなたが仕上げる。**

試作パイプライン（2026-07-26）: Playwright（Chromium）録画 → FFmpeg で MP4。  
入口は `demos/record_core_edit.py`。台本は **時代→困り→面倒→Hatlier→短い実演→締め**。LLMは呼ばない。詳細は [`demos/README.md`](./demos/README.md)。

#### 動画スタジオ構想（Hatlier同思想・Win11デスクトップ）

→ **別リポジトリ** [`D:\reel-studio`](file:///D:/reel-studio) に着手済み（段階1のWeb台本エディタまで）。  
開発ステップの正本は **`reel-studio/ROADMAP.md`**。atelier 側の録画パイプライン（`demos/record_core_edit.py`）は段階2 runner の母体として残す。

作ってみて分かったこと: 欲しいのは「Hatlier操作の録画」単体ではなく、

1. **メッセージ**（文言・尺）をアニメ／エフェクト付きで織り込む  
2. **実画面**（Hatlierなど）を途中に挟む  
3. **音声**（ナレ／BGM）を載せる  

という **タイムライン合成**。思想は Hatlier と同じく「AIが台本を書く → 人が仕上げる」。

| 層 | 中身 | 実装の当たり |
|---|---|---|
| 台本JSON | beat（card / editor / end）＋ hold ＋ voice テキスト | いまの `STORY`/`TIMING` を正本化 |
| プレビューUI | ダブルクリックで文言・尺を直す（Hatlier風） | Web UI（後で Tauri に包む） |
| 実演レイヤ | Playwright で本物UIを撮る | いまの Python ランナー |
| メッセージレイヤ | HTML/CSS アニメ（カード・タイポ） | いまの `DEMO_CSS`/`DEMO_BOOT` |
| 音声レイヤ | TTS → WAV → mux | edge-tts / Supertonic 等 |
| 書き出し | 横1280×720 ＋ 縦1080×1920 | いまの `PROFILES` |

段階: **① scenario.json 手編集→再焼** → **② ブラウザの台本エディタ** → **③ Tauriでデスクトップ化（Playwright同梱）**。本体 `hatlier.html` には依存を足さない。

**2本目以降の候補（日記・投稿フォーマット／LLM連携含む）**

| # | ストーリーの核 |
|---|---|
| 2 | 日記・旅行メモ → Webページ（文章からページへ） |
| 3 | 会社紹介 → カラー／ロゴ差し替え |
| 4 | 教材・クイズ（教育） |
| 5 | 「このボタン押したくない」→ AI修正 → 比較 → 採用 |
| 6〜 | 部品追加、SVG、ミニゲーム、レスポンシブ直し、Pages公開 など |

本数は「10本欲しい」が目標感。**最初の1〜2本の質**を先に固める。

### 既存トラックとの関係（忘れないため）

| 勝ち筋の話 | 既存のどこに落ちるか |
|---|---|
| 最後の10点を人が直す | SPEC「手動編集が第一級」＝すでに本線。デモで可視化する |
| シンプルな部品UI | Track A 手触り + C パレット制限 |
| レイアウト・部品の幅 | Track B（shell / tabs / carousel…） |
| HTML内の編集用JSON | FORMAT-v2・保存形式の検討（合意してから） |
| テンプレ・examples | Track D マイテンプレの延長 + 公式例の充実 |
| Skill / マーケットプレイス | Later（コミュニティ前提。ソロ期はローカル Skill から） |

### Later に寄せるもの（勝ち筋由来・今はやらない）

- Component / Template / AI Skill の公開マーケット
- 「ページを Apple 風に」系の差分採用エージェント
- WebGPU / Three.js / ONNX 等の **幅のアピール先行**（創れる幅は強みだが、最初のメッセージをぼかすので後）

---

## Later / 駐車場

- 置き場導線（Drive / 静的ホスト手順）
- 解析スニペット（GA4 / Umami / Plausible）
- fetch 部品（占い・ミニゲーム）
- ローカルAI・深いホスト連携
- 自由配置キャンバス（やらない寄り）
- Component / Skill マーケット、共有コミュニティ（勝ち筋 Phase 後半）
- GitHub Actions での UI テスト（開発者増員がトリガー）

---

## 開発との紐づけ

1. Issue 頭に `[Now]` / `[Format]` / `[Profile]` / `[Story]` / `[Later]`  
   （`[Story]` = デモ・examples・最初の5分・伝え方）
2. 大きな仕様は先に本ファイル or SPEC に書いてからコード
3. フォーマット破壊的変更は `data-hatlier-version` を上げ、旧文書は読み取り互換を維持
4. 勝ち筋の話で機能を足す前に、「デモ／5分体験のどれを前に進めるか」を一言決める
