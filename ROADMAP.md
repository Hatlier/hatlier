# Hatlier ロードマップ & 見える化

開発の「いま・次・あと」をここに置く。  
**リリースノートより、予定と優先度の方が先** — この文書を正本にする。

画面左上の `vX.Y.Z` はこの ROADMAP へリンクする。

---

## いまの全体像（見直し 2026-07-22）

MVPの「直す → 保存 → 書き出す」はだいたい揃った。  
これから並列で進む大きな軸は次の3つ（**同列で検討**、実装順は手触りのあとで決めてよい）。

| 軸 | ひとこと |
|---|---|
| **A. 手触り（MVP-D）** | 迷わない・壊れない・言ってることが分かる |
| **B. フォーマット拡張** | 縦スクロール一択を超え、レスポンシブを保ったまま表現の自由度を上げる |
| **C. 出力プロファイル** | 作り始めに「どんなHTMLにしたいか」を選び、部品・テンプレをそれに合わせて制限する |

**マイテンプレート**は B/C と同じレイヤ（再利用・フォーマットの延長）。置き場導線（Drive等）より、フォーマット議論とセットで扱う。

---

## 運用

| 置き場 | 役割 |
|---|---|
| **`ROADMAP.md`（本ファイル）** | 予定と優先度の正本 |
| **`SPEC.md`** | 思想・原則・詳細仕様 |
| **GitHub Releases** | 大きな区切りだけ（任意） |
| **Commit** | 何を変えたか |

毎 push にリリースノートは付けない。作業前に本ファイルの行を動かし、触れる区切りでだけ `HATLIER_VERSION` を上げる。

---

## バージョン

| 版 | 日付 | ひとこと |
|---|---|---|
| **0.5.0** | 2026-07-22 | マイテンプレ（localStorage・容量上限）＋カルーセル（CSS scroll-snap） |
| 0.4.0 | 2026-07-22 | 出力プロファイル骨格（safe/standard/rich）。デザインで説明付き選択・パレットフィルタ・書き出しに `data-profile` |
| 0.3.3 | 2026-07-22 | 手触り D1–D4。[`FORMAT-v2.md`](./FORMAT-v2.md) 仕様メモ |
| 0.3.2 | 2026-07-22 | 書き出し前にファイル名編集（`YYYYMMDDhhmm.タイトル`） |
| 0.3.1 | 2026-07-22 | PNG/動画ラスタライズ修正。MP4優先 |
| 0.3.0 | 2026-07-22 | 書き出し拡充、バージョン導線 |
| 0.2.x〜 | — | 貼る / 保存 / HTML・ZIP / 公開 |

---

## Now — 手触り（MVP-D）

- [x] D1 保存＝ブラウザ内 / 書き出し＝ファイル（文言）
- [x] D2 未書き出しの新規確認
- [x] D3 貼る結果メッセージ固定
- [x] D4 フリー編集中チップ＋案内
- [ ] D5〜 初回案内の強化、モバイル整理など（低い〜中）

仕様の大きな次: [`FORMAT-v2.md`](./FORMAT-v2.md)（フォーマット拡張＋出力プロファイル）

---

## Track B — フォーマット拡張（デザイン自由度）

### 問題意識

いまの契約は **ブロックの縦積み** が強い。レスポンシブは保ちやすいが、

- 横スクロール帯
- カルーセル
- サイドバー
- ハンバーガー／モバイルナビ

といった「ページとしての構図」が出しにくい。

### 方針（案）

1. **レスポンシブは捨てない**（モバイルで壊れないことを契約に残す）
2. 自由配置キャンバスにはしない（Figma化しない — 制約で守る思想は維持）
3. 「ブロック」に加え、**レイアウト枠（region / shell）** をフォーマットに足す方向を検討する  
   例: `shell`（ヘッダー＋メイン＋サイド）、`carousel`、`hstack`（横並びセクション）
4. 既存の縦積みドキュメントは **互換のまま開ける**（バージョン付きフォーマット）

### 検討バックログ（実装は仕様合意後）

- [ ] フォーマット v2 の骨子（`data-hatlier-version` / shell / region）
- [x] 部品: カルーセル（CSS scroll-snap, 0.5.0）
- [ ] 部品: タブ、サイドバー付きシェル、ハンバーガー
- [ ] 横スクロール・セクション（レスポンシブ時は縦に折るルール）
- [ ] AIプロンプトを v2 構造に追従
- [x] マイテンプレ最小（0.5.0）

---

## Track C — 出力プロファイル（作り始めの制約）

### 問題意識

書き出し形式や公開先で **許されるものが違う**。  
後から「CSP強いホストに上げたらスクリプトが動かない」より、**プロファイルで部品パレットとテンプレをフィルタする**方が UX として正しい。

詳細の合意は [`FORMAT-v2.md`](./FORMAT-v2.md) §3・§8。

### プロファイル（合意）

| ID | 呼び方（仮） | 制限 |
|---|---|---|
| `safe` | **いちばん無難**（**デフォルト**） | HTML+CSSのみ。**JSなし**。超強いCSPでも動く思想 |
| `standard` | ややゆるめ／ふつうの静的ホスト | 軽量JS可（現行相当）。fetchなし |
| `rich` | 拡張（Later） | fetch等。初期UIに出さなくてもよい |

CSPは実質 **強（safe）／弱（standard）**。選び方はユーザが迷わない文言に。

### UX（合意）

1. デフォルトは `safe`。**新規時の選択はスキップ可**（サジェストは急がない）
2. `doc.profile` / `data-profile` に保存
3. 部品パレット（場所クリック → 部品選択）で **使えるものだけ出す**
4. 厳しく変更するときは非互換部品を出して確認

### 部品へのタグ付け

各 `REG` に `needs: [] | ["js"] | ["media"] | ["fetch"]` など。  
`safe` は空のみ許可。

---

## Track D — マイテンプレート（B/C と同列）

| 内容 | メモ |
|---|---|
| いまの文書を「マイテンプレ」として **localStorage** に残す | **済（0.5.0）** |
| 上限は **メモリ量ベース**（件数固定より）。ユーザが変えられる | **済** — デザイン「ブラウザ設定」で KB 変更 |
| 新規時に公式テンプレと並べて選べる | **済** |
| プロファイル付きで保存（`safe` の雛形、など） | 文書スナップショットに含まれる |
| エクスポート／インポート（テンプレの受け渡し） | 未（Step 2寄りでも可） |

---

## Next（手触りの直後〜並行）

[`FORMAT-v2.md`](./FORMAT-v2.md) の §8 は合意済み。**Phase 0–1（0.4.0）・マイテンプレ＋carousel（0.5.0）実装済み**。次:

1. shell（`sidebar` / `topnav`）のどれか一本
2. tabs（CSS / details 優先）
3. `needs:["js"]` 部品が増えたときの見え方磨き
4. マイテンプレの入出力（JSON受け渡し）

書き出しの磨き（PDF余白、AGIF、置き場導線）は隙間で可。手触り D5〜 も同様。

---

## Later / 駐車場

- 置き場導線（Drive / 静的ホスト手順）
- 解析スニペット（GA4 / Umami / Plausible）
- fetch 部品（占い・ミニゲーム）
- ローカルAI・深いホスト連携
- 自由配置キャンバス（やらない寄り）

---

## 開発との紐づけ

1. Issue 頭に `[Now]` / `[Format]` / `[Profile]` / `[Later]`
2. 大きな仕様は先に本ファイル or SPEC に書いてからコード
3. フォーマット破壊的変更は `data-hatlier-version` を上げ、旧文書は読み取り互換を維持
